pip install buildozer==1.5.0
buildozer init

Edit buildozer.spec:
requirements = python3,requests,opencv-python,numpy,pillow,geopy,python-dotenv,kivy,kivymd

buildozer android debug
Install the APK from bin/ on an Android device.